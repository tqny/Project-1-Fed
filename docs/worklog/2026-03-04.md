# Worklog 2026-03-04

## Key decisions
- Prepared lightweight GitHub governance setup for solo evidence-first flow.
- Added CI workflow checks so branch protection can require `evidence-check`, `lint`, and `build`.
- Pivoted v0.1 dashboard UX from live SAM pipeline view to a synthetic lead-intelligence simulation mode for portfolio demonstration.
- Defined enterprise ICP thresholds and acceptable-distance grade policy (A/B/C considerable, D/F non-considerable) for explicit, deterministic qualification.
- Kept identity-style org fields public-reference based, while marking budget/spend/readiness pressure fields as synthetic realistic simulation.
- Rewired opportunity detail route to a lead-detail decision trail view (component scores, reasons, provenance, queue status).
- Added office-level variation for repeated organizations so larger generated batches still resemble real account pipeline structure.
- Rebalanced default synthetic grade mix toward portfolio demo needs: more `A/B`, then `C`, with minority `D/F`.
- Tuned low-band generation so `F` remains present but smaller than `D` for realistic "outside consideration" tail.
- Rolled back accidental design/bucket detour work to keep this thread scoped to ICP + synthetic lead system changes only.
- Restructured console IA into portfolio hierarchy: `Overview`, `Policy & Guardrails`, `Problem`, `System Architecture`, `Agent Workflow`, and `GitHub + Evidence`.
- Moved policy/guardrail content out of overview so landing dashboard is strictly workflow-visual (charts, filters, ranked table, run status, handoff).
- Added shared console shell/navigation component for consistent UX across all project pages.
- Applied a focused UI polish pass (depth background, elevated cards, improved KPI emphasis, cleaner stage/workflow visuals) while preserving business logic.
- Fixed styling regression where token vars were not reliably resolving in runtime by adding robust fallback tokens in `globals.css` and applying active theme class to both `html` and `body`.
- Added critical fallback CSS in root layout to guarantee styled rendering even if global stylesheet delivery is disrupted in local runtime.
- Increased visual sheen pass: added layered green glow field, stronger glare overlays on cards/background, and tuned fallback CSS specificity so fallback no longer suppresses richer global styling.
- Reverted glow tint strategy to neutral black/white sheen (removed green glow bias) while keeping glossy depth and glare treatment.
- Completed close-day evidence packaging:
  - updated `CHANGELOG.md` for synthetic pipeline + console IA release notes
  - added ADR `0003-synthetic-lead-simulation-and-portfolio-console-ia.md`
  - added demo log `2026-03-04-lead-console-hierarchy-and-synthetic-pipeline.md`

## Commands run
- `ls -la .github/workflows`
- `sed -n '1,240p' .github/workflows/evidence-check.yml`
- created `.github/workflows/ci.yml`
- `sed -n '1,260p' prisma/schema.prisma`
- `sed -n '1,260p' config/icpProfile.ts`
- `sed -n '1,320p' lib/lead-simulation.ts`
- `sed -n '1,300p' components/lead-ops-controls.tsx`
- `sed -n '1,340p' app/opportunities/page.tsx`
- `sed -n '1,320p' app/opportunities/[id]/page.tsx`
- `./scripts/init-db.sh`
- `npm run lint`
- `npm run test`
- updated `CHANGELOG.md` `[Unreleased]` entries
- created `docs/adr/0003-synthetic-lead-simulation-and-portfolio-console-ia.md`
- created `docs/demos/2026-03-04-lead-console-hierarchy-and-synthetic-pipeline.md`
- `npm run lint`
- `npm run test`
- `npm run build`
- `./scripts/check-evidence.sh`
- updated `app/globals.css` fallback token map
- updated `app/layout.tsx` to apply theme class on `body` as well
- `npm run lint`
- `npm run build`
- updated `app/globals.css` to remove green-tinted glow fields and use neutral white/black shine layers
- `npm run lint`
- `npm run build`
- updated fallback selectors in `app/layout.tsx` to low-specificity `:where(...)`
- updated `app/globals.css` with additional green glow + glare layers
- `npm run lint`
- `npm run build`
- updated `app/layout.tsx` with critical fallback style block
- `npm run lint`
- `npm run build`
- `npm run build`
- `./scripts/check-evidence.sh`
- `node --import tsx -e "import sim from './lib/lead-simulation.ts'; ..."`
- `node --import tsx -e "import sim from './lib/lead-simulation.ts'; const runs=40; ..."` (distribution checks)
- `npm run lint`
- `npm run test`
- `git checkout -- app/globals.css design-system/active-bucket.json design-system/manifest.json design-system/themes.css scripts/outfit-manager.mjs`
- `rm -rf design-system/buckets/firecrawl`
- created `components/lead-console-shell.tsx`
- rewrote `app/opportunities/page.tsx` (overview-only dashboard)
- created `app/opportunities/policy/page.tsx`
- created `app/opportunities/problem/page.tsx`
- created `app/opportunities/architecture/page.tsx`
- created `app/opportunities/agent-workflow/page.tsx`
- created `app/opportunities/github-evidence/page.tsx`
- updated `app/globals.css` for hierarchy-specific UI patterns
- `npm run lint`
- `npm run build`
- `npm run test`

## Test/build outcomes
- `./scripts/check-evidence.sh` initially failed due missing daily worklog.
- Added this worklog file; re-check pending after governance walkthrough.
- `./scripts/init-db.sh` passed (Prisma client regenerated, schema pushed to SQLite).
- `npm run lint` passed (no warnings/errors).
- `npm run test` passed (1/1 test).
- `npm run build` passed (Next.js production build succeeded).
- `./scripts/check-evidence.sh` passed.
- Lead simulation smoke check passed (`generateSyntheticLeads(80)` returned mixed A/B/C/D/F distribution and realistic sample names).
- Distribution verification (40x runs, 80 leads each) averages: `A 30.00`, `B 24.00`, `C 18.00`, `D 5.42`, `F 2.58`.
- `npm run lint` passed.
- `npm run test` passed.
- Post-hierarchy refactor `npm run lint` passed.
- Post-hierarchy refactor `npm run build` passed.
- Post-hierarchy refactor `npm run test` passed.
- Regression fix `npm run lint` passed.
- Regression fix `npm run build` passed.
- Critical fallback pass `npm run lint` passed.
- Critical fallback pass `npm run build` passed.
- Sheen/glare enhancement `npm run lint` passed.
- Sheen/glare enhancement `npm run build` passed.
- Neutral sheen pass `npm run lint` passed.
- Neutral sheen pass `npm run build` passed.
- Close-day validation `npm run lint` passed.
- Close-day validation `npm run test` passed.
- Close-day validation `npm run build` passed.
- Close-day validation `./scripts/check-evidence.sh` passed.

## Blockers/assumptions
- Assumption: GitHub UI ruleset names will match workflow job names exactly.
- Assumption: Synthetic lead generation is acceptable for portfolio storytelling as long as disclosure is explicit and consistent in UI/provenance metadata.
