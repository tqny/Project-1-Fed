# Worklog 2026-03-03

## Key decisions
- Initialized v0.1 from an empty repository with required evidence-first scaffolding before feature work.
- Selected a single Next.js App Router app with modular boundaries (`packages/connectors`, `packages/core`) to keep v0.1 simple and auditable.
- Implemented deterministic scoring as the ranking source of truth; LLM is limited to structured brief generation with safe fallback.
- Treated repository docs as hiring-signal artifacts and expanded README/changelog/ADR/demo standards accordingly.

## Commands run
- `pwd && ls -la`
- `rg --files`
- `mkdir -p .github/workflows docs/adr docs/demos docs/metrics docs/postmortems docs/worklog scripts app/opportunities/'[id]' app/api/refresh prisma packages/connectors packages/core config lib components tests`
- multiple `cat > ...` writes for scaffolding, app, connectors, and core modules
- `sed -n ...` inspections for README/changelog/demo template/worklog
- `find . -type f | sort`
- `git status --short` (reported current folder is not a git repo)

### Validation commands
- `./scripts/check-evidence.sh` => **pass**
- `npm install --no-audit --no-fund --fetch-retries=0 --fetch-timeout=20000` => **fail** (`ENOTFOUND registry.npmjs.org`)
- `npm run lint` => **fail** (`next: command not found`)
- `npm run test` => **fail** (`tsx: command not found`)
- `npm run build` => **fail** (`next: command not found`)

## Test/build outcomes
- Evidence checker passed.
- Dependency installation is currently blocked by DNS/network connectivity to npm registry.
- Lint/test/build are blocked downstream because dependencies are not installed.

## Blockers/assumptions
- Assumption: SAM and USAspending field names can vary by endpoint/version; connector normalization is defensive and logs drift.
- Current blocker: network resolution failure for `registry.npmjs.org` prevents dependency installation and runtime verification.

## Notes for close-the-day
- Do not commit or push until explicit user instruction: "close the day".
- On close-the-day, update changelog/demo/ADR as needed and run full validation set before a single daily evidence sync commit.

## Additional progress (resolved blockers)

### One-shot install strategy validated
- `NPM_CONFIG_CACHE=/tmp/npm-cache npm view next version --registry=https://registry.npmjs.org --fetch-retries=0 --fetch-timeout=10000` => **pass**
- `NPM_CONFIG_CACHE=/tmp/npm-cache npm install --no-audit --no-fund --registry=https://registry.npmjs.org --fetch-retries=0 --fetch-timeout=15000` => **pass**

### Fixes applied after validation failures
- Prisma schema indexes corrected to model-level `@@index` declarations.
- Next.js config updated to `typedRoutes` at top level.
- Explicit map item typing added in `/opportunities` table render.
- Test runner updated to `node --import tsx --test ...` to avoid tsx IPC issue.

### Final validation status
- `PRISMA_ENGINES_CACHE_DIR=/tmp/prisma-engines npm run prisma:generate` => **pass**
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

## Session continuation (install/permissions hardening)

### Install hang root cause + resolution
- Root cause: npm network DNS in sandbox caused retries with low/no output, appearing as hangs.
- Resolved with fail-fast + explicit registry + writable cache:
  - `NPM_CONFIG_CACHE=/tmp/npm-cache npm view next version --registry=https://registry.npmjs.org --fetch-retries=0 --fetch-timeout=10000`
  - `NPM_CONFIG_CACHE=/tmp/npm-cache npm install --no-audit --no-fund --registry=https://registry.npmjs.org --fetch-retries=0 --fetch-timeout=15000`

### Additional diagnostics and fixes
- `prisma db push` consistently failed with generic schema-engine error in this environment.
- Prisma 7 upgrade was tested and reverted because it requires breaking config migration (`prisma.config.ts` and datasource changes).
- Stable state restored on Prisma 6.19.2.
- As a local unblock for runtime, created SQLite `Opportunity` table and indexes directly via `sqlite3`.

### Refresh smoke check
- Executed `runRefreshPipeline(10)` via Node/TSX harness.
- Result: counts all `0`, errors include explicit message:
  - `Fetch failed: SAM_API_KEY is not configured`
- This confirms exact API-error logging path is working when key is missing.

### Final validation (post-fixes)
- `npm run prisma:generate` => **pass**
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

## Live API execution with user SAM key

### Observed SAM behavior
- Initial strict query (`postedFrom` + `postedTo` + NAICS `541511,541512,541519`) returned `totalRecords: 0`.
- Added connector fallback:
  - retry with broader 30-day window without `ncode`
  - apply NAICS filter locally
  - if still zero matches, return broad latest set with explicit warning log

### Verified refresh outcomes
- Successful live run (using `/tmp` SQLite path):
  - `countFetched: 50`
  - `countUpserted: 50`
  - `countEnriched: 0` (before enrichment counting fix)
  - `countScored: 50`
  - `countBriefed: 50`
  - `errors: []`

### Current blocker
- Subsequent SAM calls are now throttled by provider quota:
  - HTTP `429`
  - message: `You have exceeded your quota`
  - next access time reported by SAM: `2026-Mar-04 00:00:00+0000 UTC` (March 3, 2026 at 4:00 PM Pacific)

### Additional adjustments
- `countEnriched` semantics updated to count attached enrichment records, not only spend-number presence.

## Stage progression update

### Stage 1 completed: local persistence reliability
- Root issue fixed: Prisma SQLite URL was schema-relative and mispointed.
- Updated `DATABASE_URL` to `file:./dev.db` in `.env` and `.env.example`.
- Added `scripts/init-db.sh` to standardize `prisma generate` + `db push` with safe cache env.
- Verified `./scripts/init-db.sh` runs successfully end-to-end.

### Stage 2 completed: refresh run telemetry
- Added `RefreshRun` model in Prisma for auditable manual-run history.
- `runRefreshPipeline` now:
  - creates a run record at start
  - persists final status (`completed`, `completed_with_errors`, `failed`)
  - stores counters and serialized errors
  - returns `runId` in API summary
- `/opportunities` now shows latest run summary metadata.

### Validation outcomes after stage updates
- `./scripts/init-db.sh` => **pass**
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

### External API state
- SAM currently returns HTTP `429` quota throttle until `2026-Mar-04 00:00:00+0000 UTC`.

### Stage 3 completed: UX refinement for operator feedback
- Improved `/opportunities` run visibility:
  - status badge mapping (`completed`, `completed_with_errors`, `failed`, `running`)
  - latest run metadata includes run id + counts + timestamp
  - collapsible error drilldown from persisted `RefreshRun.errors`
  - parsed SAM `nextAccessTime` hint displayed when present
- Improved refresh interaction component:
  - shows run id in immediate refresh response summary
  - keeps response errors visible with collapsible detail panel
- Improved pipeline table usability:
  - added `WA` relevance column
  - added `status` column
  - added empty-state callout and clear-filters link

### Validation after Stage 3
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

### Stage 4 completed: portfolio evidence hardening
- Added demo run artifact:
  - `docs/demos/2026-03-03-refresh-pipeline-live-run.md`
- Added architectural decision record:
  - `docs/adr/0002-sam-query-fallback-and-manual-refresh-ops.md`
- Updated changelog with stage additions and UX/ops hardening notes.

### Validation after Stage 4
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

### Design wardrobe manager integration
- Installed design hub assets into `design-system/`:
  - `manifest.json`
  - all bucket folders under `design-system/buckets/`
- Added theme infrastructure:
  - `design-system/themes.css` imports all bucket token CSS files
  - `design-system/active-bucket.json` stores active outfit
  - `lib/design-system.ts` resolves active bucket and root theme class
  - `app/layout.tsx` now applies runtime root class pattern (`theme-idea-01` ... `theme-idea-05`)
- Added command parser script:
  - `scripts/outfit-manager.mjs`
  - supported commands: `list outfits`, `switch to next one`, `switch to previous one`, `switch to idea-0X`
  - package script alias: `npm run outfit -- "<command>"`
- Reworked app styling to token-driven CSS and removed inline hardcoded style literals from feature pages.
- Validation after design integration:
  - `npm run lint` => **pass**
  - `npm run test` => **pass**
  - `npm run build` => **pass**
  - `./scripts/check-evidence.sh` => **pass**

### Dashboard reset + idea-02 remap
- Activated design outfit `idea-02` (Lime Fintech Operations).
- Scrapped local data baseline:
  - cleared `Opportunity` table
  - cleared `RefreshRun` table
- Rebuilt `/opportunities` dashboard from scratch in idea-02 interaction style:
  - left ops rail navigation (`Overview`, `Lead Queue`, `Signals`)
  - KPI strip (leads in scope, high priority, due-soon, average score)
  - analysis cards with bar-based distributions
  - top agency concentration list
  - dense ranked lead queue table with explainability column
  - retained manual refresh and run telemetry/error drilldown
- Expanded token-based layout classes in `globals.css` for high-density dashboard shell, panels, bars, and responsive behavior.

### Validation after reset
- `npm run lint` => **pass**
- `npm run test` => **pass**
- `npm run build` => **pass**
- `./scripts/check-evidence.sh` => **pass**

## Close-the-day attempt
- Triggered close-the-day workflow.
- Blocker: `/Users/tqny/Documents/Project 1 - Federal Gov` is not a git repository (`fatal: not a git repository`).
- Commit and push cannot proceed until git is initialized and a remote is configured (or this folder is moved into the intended repo).
